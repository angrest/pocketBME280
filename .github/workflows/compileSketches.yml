name: Compile Sketches

# The workflow will run on every push and pull request to the repository
on:
  - push
  - pull_request

jobs:
  Compile-Sketch:
    runs-on: ubuntu-latest

    steps:
      # This step makes the contents of the repository available to the workflow
      - name: Checkout repository
        uses: actions/checkout@v2

      # For more information: https://github.com/arduino/compile-sketches#readme
      - name: Compile sketches
        uses: arduino/compile-sketches@v1
        with:
          # The default is to compile for the Arduino Uno board. If you want to compile for other boards, use the `fqbn` input.
          platforms: |
            # Install the latest release of ESP32 Boards and its toolchain via Boards Manager
            - name "esp32:esp32"
            - source-url: "https://dl.espressif.com/dl/package_esp32_index.json"
            name "esp32:esp32"

          fqbn: 'esp32:esp32:esp32s3'
          sketch-paths: |
            ./examples/ESP
